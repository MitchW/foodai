/* Optional local-only convenience: copy this file to `local-config.js` and put
 * your key in it so a local checkout works with no typing at all.
 *
 *     cp local-config.example.js local-config.js
 *
 * `local-config.js` is git-ignored. It is never committed and never reaches
 * GitHub Pages, which matters because this repository is public — a key in a
 * committed file is a key that gets scraped and drained within minutes.
 *
 * On your phone you don't need this: enter the key once in settings and the
 * browser keeps it in localStorage from then on.
 *
 * Whatever is here only SEEDS the value. Once you edit the key in the UI, the
 * saved value wins.
 */
window.SCANLAB_LOCAL_CONFIG = {
  provider: "gemini",
  key: "PASTE-YOUR-KEY-HERE"
};
